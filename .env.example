# Database configuration
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Drupal hash salt
HASH_SALT=

# Trusted host patterns (comma-separated)
TRUSTED_HOST_PATTERNS=^localhost$

# ESN Accounts OpenID Connect client credentials
ESN_ACCOUNTS_OPENID_CLIENT_ID=
ESN_ACCOUNTS_OPENID_CLIENT_SECRET=
